{
  "name": "diploma_server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "export NODE_ENV=development && set NODE_ENV=development && nodemon server.js",
    "prod": "export NODE_ENV=production && set NODE_ENV=production && nodemon server.js"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "nodemon": "^2.0.15",
    "python-shell": "^3.0.1",
    "request": "^2.88.2"
  }
}
